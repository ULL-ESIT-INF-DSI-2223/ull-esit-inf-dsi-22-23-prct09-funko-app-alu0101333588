/*
export function add(firstNumber: number, secondNumber: number) {
    return firstNumber + secondNumber;
  }
*/
/*
import { Map } from "./map";
import { AddMapReduce } from "./AddMapReduce";

/**
 * Código cliente
 * @param map objeto de la clase Map
 */
/*
function clientCode(map: Map) {
  map.run();
}

let auxiliar = clientCode(new AddMapReduce([10, 20, 30, 40, 50, 60]));
/*

/*let objeto : Map = new AddMapReduce([10, 20, 30, 40, 50, 60]);

let auxiliar_reduce : number[] = [];
auxiliar_reduce = objeto.map((elemento : number) => elemento * 2);

let auxiliar_add : number = 0;
auxiliar_add = objeto.reduce((acumulador : number, elemento : number) => acumulador + elemento, 2);

console.log(`reduce: ${objeto.map((elemento : number) => elemento * 2)}`);
console.log(`reduce: ${objeto.reduce((acumulador : number, elemento : number) => acumulador + elemento, 2)}`);
*/
